---
description: Build the app using docker compose
---

Run the following commands in sequence to build and restart the app. Wait for each command to finish before proceeding to the next one. If any command fails, stop the execution and report the error to the user.

1. `docker compose build`
2. `docker compose down`
3. `docker compose up -d`
